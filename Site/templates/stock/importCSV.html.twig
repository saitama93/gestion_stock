{% extends "base.html.twig" %}
{% block body %}
    <div class="well">
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="{{ path('Materiel.index') }}">Stock</a></li>
                <li class="breadcrumb-item active" aria-current="page">ImportCSV</li>
            </ol>
        </nav>
        <a href="{{ path('Materiel.index') }}" class="btn btn-danger">Annuler</a>
        <h1>Attention</h1>
        <h2>Si vous rajoutez de nouveau matériel via cette méthode veillez à :</h2>
        <p>- Éviter les fautes d'orthographe sur toutes les données</p>
        <p>- Ne pas avoir de ligne vide</p>
        <small>TIPS : Vous pouvez uniquement ici rajouter de nouveau matériel sans modifier les autres existants</small>
        <br>
        <small>Les modèles de CSV se trouvent dans /public/csv</small>
        <br>
        <small>Ce texte est modifiable dans /templates/stock/importCSV.html.twig</small>
        <form method="post" enctype="multipart/form-data">
            <input type="file" name="csvFile">
            <br>
            <a href="{{ path('Materiel.downloadModel') }}" class="btn btn-primary">Télécharger le modèle de CSV</a>
            <input type="submit" name="envoyer" value="Mettre à jour" class="btn btn-warning">
        </form>
    </div>

{% endblock %}