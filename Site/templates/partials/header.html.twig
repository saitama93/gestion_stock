{# NavBar #}


<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<a class="navbar-brand" href="{{ path('Home.index') }}" id="image_site"><img src="../../image/LOGO_CD25.png" width="148" height="40"></a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>

	<div class="collapse navbar-collapse" id="navbarColor01">
		<ul class="navbar-nav mr-auto">
			<li class="nav-item">
				<a class="nav-link" href="{{ path('Intervention.index') }}">
					<i class="fas fa-chart-line"></i>
					Intervention
				</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="{{ path('Materiel.index') }}">
					<i class="fas fa-hotel"></i>
					Stock
				</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="{{ path('Historique.index') }}">
					<i class="fas fa-folder"></i>
					Historique
				</a>
			</li>
		</ul>
		<ul class="navbar-nav ml-auto">
			{% if app.user %}
				<li class="nav-item dropdown">
					<a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropdownLink">
						{{ app.user.nom }}
					</a>
					<div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
						<a href="#" class="dropdown-item">Départ en intervention</a>
						<a href="#" class="dropdown-item">Retour d'intervention</a>

						<div class="dropdown-divider"></div>

						<a href="#" class="dropdown-item">Mon compte</a>
						<a href="#" class="dropdown-item">Modifier mon profil</a>

						<div class="dropdown-divider"></div>

						{# <p>Gestions</p> #}
						<a href="{{ path('AdminDasboard.index') }}" class="dropdown-item">Gestions</a>
						{# <a href="#" class="dropdown-item">Gestion Utilisateurs</a>
						<a href="#" class="dropdown-item">Gestion Interventions</a>
						<a href="#" class="dropdown-item">Gestion Materiels</a>
						<a href="#" class="dropdown-item">Gestion Marques</a>
						<a href="#" class="dropdown-item">Gestion Statuts</a>
						<a href="#" class="dropdown-item">Gestion Types</a>
						<a href="#" class="dropdown-item">Gestion Spécificités</a>
						<a href="#" class="dropdown-item">Gestion Sites</a> #}

						<div class="dropdown-divider"></div>

						<a href="#" class="dropdown-item text-danger">Déconnexion</a>
					</div>
				</li>
			{% endif %}
		</ul>
	</div>
</nav>

{% for label, messages in app.flashes %}

	<div class="container">
		<div class="alert alert-{{ label }}">
			{% for message in messages %}
				<p>{{ message | raw }}</p>
			{% endfor %}
		</div>
	</div>
{% endfor %}


{# Messages Flash #}
{% for label, messages in app.flashes %}

	<div class="container">
		<div class="alert alert-{{ label }}">
			{% for message in messages %}
				<p>{{ message | raw }}</p>
			{% endfor %}
		</div>
	</div>
{% endfor %}
